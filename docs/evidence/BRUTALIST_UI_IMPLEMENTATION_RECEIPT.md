# Brutalist UI implementation receipt

This receipt is intentionally claim-bounded. The branch introduces the oversized editorial-brutalist interface system and its deterministic product mark. It may be called source-ready only after the exact branch passes the ordinary five-job CI matrix. It may be called canonical only after squash merge and exact-main CI success.

Target-machine M1, L1, X1 and C1 evidence remains independent and open until representative hardware proves it.
